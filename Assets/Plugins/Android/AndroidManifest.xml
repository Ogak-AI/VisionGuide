<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yourcompany.visionguide">

    <!-- Required permissions for Meta Horizon OS passthrough and camera access -->
    <uses-permission android:name="horizonos.permission.HEADSET_CAMERA" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- NOTE: This manifest is a minimal template. Unity and XR plugins may require additional
         application/activity attributes and meta-data. Merge this into your final manifest
         or place it at Assets/Plugins/Android/AndroidManifest.xml so Unity's Gradle build will include it. -->

    <application android:label="@string/app_name" android:icon="@mipmap/ic_launcher">
        <activity android:name="com.unity3d.player.UnityPlayerActivity"
                  android:label="@string/app_name"
                  android:exported="true"
                  android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
